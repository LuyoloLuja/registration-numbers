function regFactory(){
  var regNumbers = [];

  function regSelection(selectReg){
    if(selectReg.startsWith("CA") || selectReg.startsWith("CJ") || selectReg.startsWith("CAG") || selectReg.startsWith("CL")){
      
    }
  }
}
